import { Couponmanagement } from './couponmanagement.model';

describe('Couponmanagement', () => {
  it('should create an instance', () => {
    expect(new Couponmanagement()).toBeTruthy();
  });
});
